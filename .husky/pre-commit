#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo '🏗️ Проверяем код перед коммитом...'

# Проверяем форматирование
npm run format:check || {
    echo '❌ Ошибка форматирования. Запустите npm run format и повторите коммит.'
    exit 1
}

# Проверяем линтером
npm run lint:check || {
    echo '❌ Ошибки линтера. Исправьте ошибки и повторите коммит.'
    exit 1
}

# Проверяем типы
npm run typecheck || {
    echo '❌ Ошибки типов. Исправьте ошибки и повторите коммит.'
    exit 1
}

echo '✅ Проверки пройдены успешно'
