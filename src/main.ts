/**
 * Основной файл приложения Amazing List
 * @module main
 * @description Инициализация Vue приложения и основных зависимостей
 */

import { createApp } from 'vue'
import App from './App.vue'

const app = createApp(App)
app.mount('#app')
